import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Exterior Waterproofing Tampa | Building Envelope Protection",
  description: "Professional exterior waterproofing for Tampa Bay properties. Elastomeric coatings, joint sealants, deck waterproofing, and comprehensive building envelope solutions.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
