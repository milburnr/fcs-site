import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Insurance Claims Process | Disaster Recovery Claim Assistance",
  description: "Expert guidance through the insurance claims process for disaster recovery. Documentation, adjuster coordination, and claim maximization. 43+ years helping Tampa Bay property owners.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
