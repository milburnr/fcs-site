import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Water Damage Restoration Tampa | Flood & Leak Repair Experts",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
