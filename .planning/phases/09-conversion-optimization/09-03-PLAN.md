---
phase: 09-conversion-optimization
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - app/insurance/guides/*/page.tsx
  - app/residential/guides/*/page.tsx
autonomous: true

must_haves:
  truths:
    - "All ~46 insurance guide pages use GuideCTA with silo='insurance'"
    - "All ~25 residential guide pages use GuideCTA with silo='residential'"
    - "Every guide page across all silos now uses the GuideCTA component instead of inline CTA banners"
  artifacts:
    - path: "app/insurance/guides/fire-damage-restoration-and-insurance-in-tampa/page.tsx"
      provides: "Representative updated insurance guide page"
      contains: "GuideCTA"
    - path: "app/residential/guides/tampa-luxury-custom-home-building-services/page.tsx"
      provides: "Representative updated residential guide page"
      contains: "GuideCTA"
  key_links:
    - from: "app/insurance/guides/*/page.tsx"
      to: "components/GuideCTA.tsx"
      via: "import and JSX usage"
      pattern: "import.*GuideCTA"
    - from: "app/residential/guides/*/page.tsx"
      to: "components/GuideCTA.tsx"
      via: "import and JSX usage"
      pattern: "import.*GuideCTA"
---

<objective>
Replace inline green CTA banner sections across all ~46 insurance and ~25 residential guide pages with the GuideCTA component.

Purpose: Completes CTA-02 for the remaining two guide silos (insurance + residential = 71 pages), achieving full guide CTA standardization.
Output: ~71 updated guide pages across insurance and residential silos
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-conversion-optimization/09-RESEARCH.md
@.planning/phases/09-conversion-optimization/09-01-SUMMARY.md
@components/GuideCTA.tsx
@scripts/replace-guide-ctas.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run CTA replacement script on insurance and residential guides</name>
  <files>
    app/insurance/guides/*/page.tsx
    app/residential/guides/*/page.tsx
  </files>
  <action>
Using the `scripts/replace-guide-ctas.js` script created in Plan 09-02, run it for the insurance and residential silos:

```bash
node scripts/replace-guide-ctas.js --silo insurance
node scripts/replace-guide-ctas.js --silo residential
```

If the script encounters any issues specific to insurance or residential page structures (different CTA markup patterns), fix the script to handle those variants before re-running.

Insurance guides (46 pages) should get `<GuideCTA silo="insurance" />`.
Residential guides (25 pages) should get `<GuideCTA silo="residential" />`.

Same rules as Plan 02:
- Preserve topic-specific headings as `heading` prop overrides
- Only replace the BOTTOM green CTA section
- Remove unused BUSINESS_INFO imports
- Skip files already using GuideCTA

After running both silos, also check for any remaining guide pages that might exist in other paths (e.g., legacy guides under `/app/` root that weren't moved to silos). Search for: `grep -rl "Get Free Estimate" app/ --include="page.tsx" | grep -v "/guides/" | grep -v "/services/" | grep -v "node_modules"`. If any non-guide pages have generic "Get Free Estimate" CTAs, note them in the summary but do NOT change them here (they're not guide pages).
  </action>
  <verify>
1. Run: `grep -rl "GuideCTA" app/insurance/guides/ | wc -l` -- should be ~46
2. Run: `grep -rl "GuideCTA" app/residential/guides/ | wc -l` -- should be ~25
3. Run: `npm run build 2>&1 | tail -10` -- build succeeds
4. Total GuideCTA adoption: `grep -rl "GuideCTA" app/*/guides/ | wc -l` -- should be ~262
  </verify>
  <done>All insurance and residential guide pages use GuideCTA component with correct silo props, build passes, and all ~262 guides across all silos are standardized</done>
</task>

</tasks>

<verification>
1. `grep -rl "GuideCTA" app/insurance/guides/ | wc -l` returns ~46
2. `grep -rl "GuideCTA" app/residential/guides/ | wc -l` returns ~25
3. `npm run build` succeeds
4. Combined: all 3 silos have GuideCTA adoption
</verification>

<success_criteria>
- All insurance guides use GuideCTA with silo="insurance"
- All residential guides use GuideCTA with silo="residential"
- Build passes with no errors
- CTA-02 fully satisfied across all guide silos
</success_criteria>

<output>
After completion, create `.planning/phases/09-conversion-optimization/09-03-SUMMARY.md`
</output>
