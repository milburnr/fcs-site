---
phase: 05-location-page-strengthening
plan: 09
type: execute
wave: 3
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05", "05-06", "05-07", "05-08"]
files_modified:
  - app/locations/tampa-fl/page.tsx
  - app/locations/st-petersburg-fl/page.tsx
  - app/locations/clearwater-fl/page.tsx
  - app/locations/lakeland-fl/page.tsx
  - app/locations/sarasota-fl/page.tsx
  - app/locations/bradenton-fl/page.tsx
  - app/locations/brandon-fl/page.tsx
  - app/locations/ruskin-fl/page.tsx
  - lib/constants.ts
autonomous: true

must_haves:
  truths:
    - "All 8 location hub pages link to ALL 9 service+location pages for that city"
    - "All 72 service+location pages link back to their city's location hub via NearbyLocations or explicit link"
    - "The generateLocationFAQs() function is removed from lib/constants.ts"
    - "npm run build succeeds with all 80 pages (72 service+location + 8 hubs)"
  artifacts:
    - path: "app/locations/tampa-fl/page.tsx"
      provides: "Tampa location hub with links to all 9 Tampa service+location pages"
      contains: "commercial-construction-tampa"
    - path: "app/locations/ruskin-fl/page.tsx"
      provides: "Ruskin location hub with links to all 9 Ruskin service+location pages"
      contains: "commercial-construction-ruskin"
  key_links:
    - from: "app/locations/*/page.tsx"
      to: "app/*-[city]/page.tsx"
      via: "Link href to service+location pages"
      pattern: "href=\"/[a-z-]+-tampa/\""
---

<objective>
Update all 8 location hub pages to link to their city's 9 service+location pages (LOC-07 bidirectional linking), and remove the unused generateLocationFAQs() function from lib/constants.ts.

Purpose: LOC-07 requires bidirectional linking between location hubs and service+location pages. The location hubs currently do NOT link to service+location pages — this is the final structural piece. Also, generateLocationFAQs() is dead code now that all 72 pages have inline FAQs.
Output: 8 updated location hub pages with service+location links, and cleaned-up constants.ts.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-location-page-strengthening/05-RESEARCH.md
@.planning/phases/05-location-page-strengthening/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add service+location links to all 8 location hub pages</name>
  <files>
    app/locations/tampa-fl/page.tsx
    app/locations/st-petersburg-fl/page.tsx
    app/locations/clearwater-fl/page.tsx
    app/locations/lakeland-fl/page.tsx
    app/locations/sarasota-fl/page.tsx
    app/locations/bradenton-fl/page.tsx
    app/locations/brandon-fl/page.tsx
    app/locations/ruskin-fl/page.tsx
  </files>
  <action>
For each of the 8 location hub pages, add a section linking to ALL 9 service+location pages for that city. This completes LOC-07 (bidirectional linking).

**Pattern for each hub page:**

1. Read the current hub page content
2. Add a "Our Services in [City]" section with links to all 9 service+location pages
3. Use a clean grid or list layout with descriptive link text

**Service+location page URL pattern:**
For each city, the 9 service pages are:
- `/commercial-construction-[city]/`
- `/multi-family-construction-[city]/`
- `/condo-remediation-[city]/`
- `/insurance-restoration-[city]/`
- `/disaster-recovery-[city]/`
- `/historic-restoration-[city]/`
- `/luxury-custom-homes-[city]/`
- `/balcony-reconstruction-[city]/`
- `/exterior-waterproofing-[city]/`

Where `[city]` is the URL slug: `tampa`, `st-petersburg`, `clearwater`, `lakeland`, `sarasota`, `bradenton`, `brandon`, `ruskin`.

**Implementation:**
- Add the service links section BEFORE the existing CTA section
- Use Next.js `Link` component for internal links
- Style as a card grid or organized list that fits the existing hub page design
- Use descriptive labels: "Commercial Construction in Tampa", "Condo Remediation in Tampa", etc.
- DO NOT rewrite the rest of the hub page — only ADD the service+location links section

**Bidirectional check:** The service+location pages already have `NearbyLocations` component which links to the location hub. Verify that component is present on the rewritten pages. If any pages are missing NearbyLocations, note in the summary.
  </action>
  <verify>
Run `npm run build`. All 8 location hub pages compile. Grep each hub page for all 9 service+location URLs to confirm links are present. Example: `grep "commercial-construction-tampa" app/locations/tampa-fl/page.tsx` should match.
  </verify>
  <done>All 8 location hub pages link to all 9 service+location pages for their city. Bidirectional linking complete.</done>
</task>

<task type="auto">
  <name>Task 2: Remove generateLocationFAQs() and final build verification</name>
  <files>
    lib/constants.ts
  </files>
  <action>
1. Open `lib/constants.ts`
2. Find the `generateLocationFAQs()` function (around lines 468-498 per research)
3. Remove the entire function — it is dead code (no page imports or calls it)
4. Verify no file imports or references `generateLocationFAQs` anywhere in the codebase:
   - Run grep across all page files for `generateLocationFAQs`
   - If any reference is found, remove the import/call and ensure the page uses its inline FAQ array instead
5. Run full build to confirm nothing breaks

**Also verify the full phase state:**
- Spot-check 3-4 random service+location pages from different cities to confirm they have:
  - Unique FAQs (inline, not from generateLocationFAQs)
  - ContentParallax visual break
  - RelatedServices + NearbyLocations components
  - LocalBusinessSchema, ServiceSchema, BreadcrumbSchema
  - Metadata with canonical URL
  </action>
  <verify>
Run `npm run build` from site root. Full build must succeed with all pages. Run `grep -r "generateLocationFAQs" app/` — should return zero results.
  </verify>
  <done>generateLocationFAQs() removed from lib/constants.ts. No remaining references in any page file. Full build passes.</done>
</task>

</tasks>

<verification>
1. All 8 location hub pages have links to their 9 service+location pages
2. Service+location pages link back to location hubs via NearbyLocations
3. generateLocationFAQs() removed from lib/constants.ts
4. No remaining references to generateLocationFAQs in any file
5. Full build succeeds
</verification>

<success_criteria>
LOC-07 bidirectional linking complete across all 8 cities. Dead code removed. Full site builds successfully with all 72 rewritten pages and 8 updated hub pages.
</success_criteria>

<output>
After completion, create `.planning/phases/05-location-page-strengthening/05-09-SUMMARY.md`
</output>
