---
phase: 06-e-e-a-t-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/AuthorByline.tsx
  - components/CodeReference.tsx
  - components/ProjectGallery.tsx
autonomous: true

must_haves:
  truths:
    - "AuthorByline component renders company name with optional publish/modified dates"
    - "CodeReference component renders styled callout box with code citation, description, and external link"
    - "ProjectGallery component renders responsive photo grid using OptimizedImage with hover captions"
  artifacts:
    - path: "components/AuthorByline.tsx"
      provides: "Company byline component for article pages"
      contains: "AuthorByline"
    - path: "components/CodeReference.tsx"
      provides: "Building code callout component"
      contains: "CodeReference"
    - path: "components/ProjectGallery.tsx"
      provides: "Project photo gallery component"
      contains: "ProjectGallery"
  key_links:
    - from: "components/AuthorByline.tsx"
      to: "lib/constants.ts"
      via: "BUSINESS_INFO.name import"
      pattern: "BUSINESS_INFO"
    - from: "components/ProjectGallery.tsx"
      to: "components/OptimizedImage.tsx"
      via: "OptimizedImage import"
      pattern: "OptimizedImage"
---

<objective>
Create the three reusable components that Phase 6 will layer across the site: AuthorByline for article pages, CodeReference for building code callouts, and ProjectGallery for service page photo sections.

Purpose: These are the foundation components all subsequent plans depend on.
Output: Three production-ready components in components/ directory.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e-e-a-t-signals/06-RESEARCH.md
@components/OptimizedImage.tsx
@components/ContentImage.tsx
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthorByline, CodeReference, and ProjectGallery components</name>
  <files>components/AuthorByline.tsx, components/CodeReference.tsx, components/ProjectGallery.tsx</files>
  <action>
Create three components following the designs in 06-RESEARCH.md:

**AuthorByline.tsx:**
- Imports BUSINESS_INFO from @/lib/constants
- Props: publishDate?: string, modifiedDate?: string, variant?: "light" | "dark" (default "dark")
- Renders: "By {BUSINESS_INFO.name}" with optional date display
- Dark variant: text-gray-500 for body sections
- Light variant: text-gray-300 for hero sections on dark backgrounds
- Simple flex layout with pipe separators between elements
- No bio blurb (per locked decision: name only, credentials already in trust line/footer)

**CodeReference.tsx:**
- Props: code (string), section? (string), description (string), link? (string), linkText? (string, default "View Source")
- Styled callout box: bg-blue-50, border-l-4 border-blue-600, rounded-r-lg
- Code name in bold, section in medium weight, description in regular text
- External link opens in new tab with rel="noopener noreferrer"
- Uses the border-l-4 callout pattern already present across 15+ pages in the codebase

**ProjectGallery.tsx:**
- Imports OptimizedImage from @/components/OptimizedImage
- Props: title? (string, default "Our Recent Projects"), subtitle? (string), photos (GalleryPhoto[]), columns? (2|3|4, default 3)
- GalleryPhoto type: { src: string, caption?: string }
- Responsive grid using section and container-custom classes
- Each photo in aspect-[4/3] with rounded-xl shadow-lg
- Hover effect: scale-105 on image, caption fades in from gradient overlay
- Uses OptimizedImage with size="medium" for all photos
  </action>
  <verify>
Run `npm run build 2>&1 | tail -5` to confirm clean build. Then verify each component file exists and exports the expected component:
- `grep "export function AuthorByline" components/AuthorByline.tsx`
- `grep "export function CodeReference" components/CodeReference.tsx`
- `grep "export function ProjectGallery" components/ProjectGallery.tsx`
  </verify>
  <done>All three components exist, export correctly, and the site builds without errors.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no TypeScript errors
- Each component file exports its named function
- AuthorByline uses BUSINESS_INFO.name (not hardcoded string)
- CodeReference external links have target="_blank" rel="noopener noreferrer"
- ProjectGallery uses OptimizedImage (not raw img tags)
</verification>

<success_criteria>
Three new components created and build-verified. Ready for consumption by all subsequent Phase 6 plans.
</success_criteria>

<output>
After completion, create `.planning/phases/06-e-e-a-t-signals/06-01-SUMMARY.md`
</output>
