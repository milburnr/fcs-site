---
phase: 06-e-e-a-t-signals
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - scripts/add-bylines.js
  - app/commercial/guides/*/page.tsx
  - app/insurance/guides/*/page.tsx
  - app/residential/guides/*/page.tsx
autonomous: true

must_haves:
  truths:
    - "Every guide article page (~262 pages) has an AuthorByline component rendered below the h1"
    - "The byline displays 'By Florida Construction Specialists' on every guide page"
  artifacts:
    - path: "scripts/add-bylines.js"
      provides: "Batch byline insertion script"
      contains: "AuthorByline"
    - path: "app/commercial/guides/*/page.tsx"
      provides: "Commercial guides with bylines"
      contains: "AuthorByline"
  key_links:
    - from: "app/*/guides/*/page.tsx"
      to: "components/AuthorByline.tsx"
      via: "import statement"
      pattern: "AuthorByline"
---

<objective>
Batch-add the AuthorByline component to all ~262 guide article pages across the three silos (commercial, insurance, residential).

Purpose: Every article page gets a company byline per the locked decision. This is a mechanical, scriptable operation.
Output: All guide pages import and render AuthorByline. Reusable script for future articles.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-e-e-a-t-signals/06-01-SUMMARY.md
@.planning/phases/06-e-e-a-t-signals/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run batch byline insertion script</name>
  <files>scripts/add-bylines.js, app/commercial/guides/*/page.tsx, app/insurance/guides/*/page.tsx, app/residential/guides/*/page.tsx</files>
  <action>
Create `scripts/add-bylines.js` that:
1. Scans all page.tsx files in `app/commercial/guides/*/`, `app/insurance/guides/*/`, and `app/residential/guides/*/`
2. For each file:
   a. Skip if already contains "AuthorByline" (idempotent)
   b. Add `import { AuthorByline } from "@/components/AuthorByline";` to the imports section (after existing component imports)
   c. Find the first `<h1` element and insert `<AuthorByline />` on the line after the closing `</h1>` tag (or after the closing `>` if h1 is self-closing on one line)
   d. If the h1 spans multiple lines, find the closing tag line and insert after it
3. Track success/skip/error counts and log summary

The script should handle the common article patterns seen in the codebase:
- Single-line h1: `<h1 className="...">Title</h1>` -> insert AuthorByline on next line
- Multi-line h1: find the line with `</h1>` and insert after it
- The component should be inserted at the same indentation level as the h1

Run the script: `node scripts/add-bylines.js`

After running, verify with: `grep -rL "AuthorByline" app/commercial/guides/*/page.tsx app/insurance/guides/*/page.tsx app/residential/guides/*/page.tsx 2>/dev/null | wc -l` (should be 0)

Then run `npm run build` to verify no build errors.
  </action>
  <verify>
- `grep -rL "AuthorByline" app/commercial/guides/*/page.tsx | wc -l` returns 0
- `grep -rL "AuthorByline" app/insurance/guides/*/page.tsx | wc -l` returns 0
- `grep -rL "AuthorByline" app/residential/guides/*/page.tsx | wc -l` returns 0
- `npm run build` succeeds
  </verify>
  <done>All 262 guide article pages import and render AuthorByline. Zero guide pages missing the byline. Build passes.</done>
</task>

</tasks>

<verification>
- `grep -rc "AuthorByline" app/commercial/guides/*/page.tsx | grep -c ":0$"` returns 0 (no files missing it)
- Same check for insurance/guides and residential/guides
- `npm run build` succeeds with no errors
- Spot check 3 random articles to confirm byline renders correctly in markup
</verification>

<success_criteria>
All ~262 guide articles have AuthorByline component imported and rendered below the h1. Build passes clean.
</success_criteria>

<output>
After completion, create `.planning/phases/06-e-e-a-t-signals/06-02-SUMMARY.md`
</output>
