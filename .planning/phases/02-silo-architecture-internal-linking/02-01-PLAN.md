---
phase: 02-silo-architecture-internal-linking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/commercial/page.tsx
  - app/commercial-construction/page.tsx
  - app/services/commercial/page.tsx
  - app/residential/page.tsx
  - app/custom-homes/page.tsx
  - app/services/residential/page.tsx
  - app/insurance/page.tsx
  - app/insurance-restoration/page.tsx
  - app/disaster-recovery-insurance-restoration/page.tsx
  - app/services/disaster-recovery/page.tsx
  - netlify.toml
autonomous: true

must_haves:
  truths:
    - "/commercial/ serves a 200 with self-referencing canonical and is the sole commercial hub"
    - "/residential/ serves a 200 with self-referencing canonical and is the sole residential hub"
    - "/insurance/ serves a 200 with self-referencing canonical and is the sole insurance hub"
    - "All loser hub pages are deleted and have 301 redirects to the winner"
    - "No existing redirects in netlify.toml chain through a now-deleted loser URL"
  artifacts:
    - path: "netlify.toml"
      provides: "301 redirects for all deleted hub duplicates plus updated Phase 1 redirect targets"
      contains: "from = \"/commercial-construction/\""
    - path: "app/commercial/page.tsx"
      provides: "Authoritative commercial hub with self-referencing canonical"
      contains: "canonical"
  key_links:
    - from: "netlify.toml"
      to: "app/commercial/page.tsx"
      via: "No redirect FROM /commercial/ exists (line 644 removed)"
      pattern: "commercial"
---

<objective>
Consolidate duplicate hub pages for all three business lines (commercial, residential, insurance) to single authoritative URLs.

Purpose: Google currently sees 3 commercial URLs, 4 residential URLs, and 5 insurance URLs competing for the same keywords. Hub consolidation eliminates this cannibalization and establishes the silo roots that articles will nest under in subsequent plans.

Output: Three clean hub pages with self-referencing canonicals, all loser pages deleted with 301 redirects, and no redirect chains in netlify.toml.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-silo-architecture-internal-linking/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate hub pages and fix canonicals</name>
  <files>
    app/commercial/page.tsx
    app/commercial-construction/page.tsx
    app/services/commercial/page.tsx
    app/residential/page.tsx
    app/custom-homes/page.tsx
    app/services/residential/page.tsx
    app/insurance/page.tsx
    app/insurance-restoration/page.tsx
    app/disaster-recovery-insurance-restoration/page.tsx
    app/services/disaster-recovery/page.tsx
  </files>
  <action>
    **Commercial hub consolidation:**
    - WINNER: `/commercial/` (30KB, comprehensive, in NAV_ITEMS)
    - Fix `/commercial/page.tsx` canonical: change from `/commercial-construction/` to self-referencing `https://floridaconstructionspecialists.com/commercial/`
    - Check if `/commercial-construction/page.tsx` or `/services/commercial/page.tsx` have any unique content NOT in `/commercial/page.tsx`. If so, merge that content into the winner before deleting.
    - Delete `/commercial-construction/page.tsx` (and its directory if empty)
    - Delete `/services/commercial/page.tsx` (and its directory if it becomes empty -- check for sibling pages under /services/ first)

    **Residential hub consolidation:**
    - WINNER: `/residential/` (in NAV_ITEMS, silo root)
    - Fix `/residential/page.tsx` canonical to self-referencing `https://floridaconstructionspecialists.com/residential/`
    - KEEP `/luxury-custom-homes/` -- it is a distinct service page, NOT a hub duplicate
    - Check `/custom-homes/page.tsx` for unique content, merge if needed
    - Delete `/custom-homes/page.tsx` (and its directory)
    - Delete `/services/residential/page.tsx` (and its directory if empty)

    **Insurance hub consolidation:**
    - WINNER: `/insurance/` (in NAV_ITEMS, has sub-services nested)
    - Fix `/insurance/page.tsx` canonical to self-referencing `https://floridaconstructionspecialists.com/insurance/`
    - KEEP `/disaster-recovery/` -- it is a distinct service page about disaster recovery work, NOT a duplicate of insurance
    - Check `/insurance-restoration/page.tsx` for unique content, merge if needed
    - Delete `/insurance-restoration/page.tsx` (and its directory)
    - Delete `/disaster-recovery-insurance-restoration/page.tsx` (and its directory)
    - Delete `/services/disaster-recovery/page.tsx` (and its directory if empty)

    **Important:** When deleting pages, use `rm -rf` on the page directory. Do NOT delete parent directories that contain other pages (e.g., `/services/` has other children).
  </action>
  <verify>
    Verify winner pages exist and have self-referencing canonicals:
    - `grep -r "canonical" app/commercial/page.tsx` shows `/commercial/`
    - `grep -r "canonical" app/residential/page.tsx` shows `/residential/`
    - `grep -r "canonical" app/insurance/page.tsx` shows `/insurance/`
    Verify losers are deleted:
    - `ls app/commercial-construction/page.tsx` should fail
    - `ls app/services/commercial/page.tsx` should fail
    - `ls app/custom-homes/page.tsx` should fail
    - `ls app/services/residential/page.tsx` should fail
    - `ls app/insurance-restoration/page.tsx` should fail
    - `ls app/disaster-recovery-insurance-restoration/page.tsx` should fail
    - `ls app/services/disaster-recovery/page.tsx` should fail
  </verify>
  <done>Three authoritative hub pages exist at /commercial/, /residential/, /insurance/ with self-referencing canonicals. Seven loser pages deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Add redirects and fix redirect chains</name>
  <files>netlify.toml</files>
  <action>
    **Step 1: Remove the conflicting redirect at line ~644 of netlify.toml:**
    Find and DELETE the existing redirect block:
    ```toml
    [[redirects]]
      from = "/commercial/"
      to = "/commercial-construction/"
      status = 301
    ```
    This redirect actively sends visitors away from the intended authoritative commercial hub.

    **Step 2: Add 301 redirects for all deleted loser pages.**
    Insert these redirect blocks BEFORE the catch-all `/*` redirect at the bottom:
    ```toml
    [[redirects]]
      from = "/commercial-construction/"
      to = "/commercial/"
      status = 301

    [[redirects]]
      from = "/services/commercial/"
      to = "/commercial/"
      status = 301

    [[redirects]]
      from = "/custom-homes/"
      to = "/residential/"
      status = 301

    [[redirects]]
      from = "/services/residential/"
      to = "/residential/"
      status = 301

    [[redirects]]
      from = "/insurance-restoration/"
      to = "/insurance/"
      status = 301

    [[redirects]]
      from = "/disaster-recovery-insurance-restoration/"
      to = "/insurance/"
      status = 301

    [[redirects]]
      from = "/services/disaster-recovery/"
      to = "/insurance/"
      status = 301
    ```

    **Step 3: Fix redirect chains from Phase 1.**
    Scan ALL existing redirect entries in netlify.toml. Any redirect whose `to` value matches a URL we just redirected (e.g., `to = "/commercial-construction/"` or `to = "/insurance-restoration/"` or `to = "/services/commercial/"`) must be updated to point directly to the final destination. For example:
    - `to = "/commercial-construction/"` becomes `to = "/commercial/"`
    - `to = "/services/commercial/"` becomes `to = "/commercial/"`
    - `to = "/insurance-restoration/"` becomes `to = "/insurance/"`
    - `to = "/services/disaster-recovery/"` becomes `to = "/insurance/"`
    - `to = "/custom-homes/"` becomes `to = "/residential/"`
    - `to = "/services/residential/"` becomes `to = "/residential/"`

    **Step 4: Also update any internal link references across all page.tsx files** that link to the old loser URLs. Grep the entire app/ directory for href references to `/commercial-construction/`, `/services/commercial/`, `/custom-homes/`, `/services/residential/`, `/insurance-restoration/`, `/disaster-recovery-insurance-restoration/`, `/services/disaster-recovery/` and update them to their winner URLs.
  </action>
  <verify>
    - `grep "commercial-construction" netlify.toml` shows only the redirect FROM it, never as a TO target
    - `grep -r "href.*commercial-construction" app/` returns zero matches (all internal links updated)
    - `grep -r "href.*services/commercial" app/` returns zero matches
    - `grep -r "href.*custom-homes" app/` returns zero matches (except if luxury-custom-homes exists -- that is OK)
    - Count redirect blocks in netlify.toml to confirm 7 new ones added
  </verify>
  <done>Seven new 301 redirects added for deleted hub pages. No redirect chains exist (all Phase 1 redirects point to final destinations). All internal links across all page.tsx files updated to use winner hub URLs.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors (confirms no broken imports from deleted pages)
2. Three hub pages build at /commercial/, /residential/, /insurance/
3. No page.tsx files reference deleted loser URLs in href attributes
4. netlify.toml has no redirect chains (no redirect's `to` value is itself a `from` in another redirect)
</verification>

<success_criteria>
- /commercial/, /residential/, /insurance/ each have self-referencing canonical URLs
- 7 duplicate hub pages deleted
- 7 new 301 redirects in netlify.toml
- Zero redirect chains
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-silo-architecture-internal-linking/02-01-SUMMARY.md`
</output>
