---
phase: 01-content-audit-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - netlify.toml
  - scripts/cleanup-placeholder-pages.js
autonomous: true

must_haves:
  truths:
    - "The fishing charters page is gone with a 301 redirect to a relevant location page"
    - "All 128 placeholder pages that already have redirects in netlify.toml have their source page.tsx files deleted"
    - "No placeholder pages (34-word 'Redirecting...' stubs) remain as indexable content"
  artifacts:
    - path: "netlify.toml"
      provides: "301 redirect for fishing charters page"
      contains: "poseidon-fishing-charters"
    - path: "scripts/cleanup-placeholder-pages.js"
      provides: "Script that cross-references placeholders with existing redirects"
  key_links:
    - from: "netlify.toml"
      to: "deleted page directories"
      via: "301 redirects fire before static file serving"
      pattern: "status = 301"
---

<objective>
Remove the off-topic fishing charters page and clean up ~128 placeholder page.tsx files that are just redirect stubs.

Purpose: Eliminate off-topic content diluting topical authority (AUDIT-03) and remove dead-weight placeholder files that inflate page count without adding value (partial AUDIT-02). This reduces the page count from ~496 to ~367, making downstream plans (keyword mapping, description rewrites) much more manageable.
Output: netlify.toml with fishing charters redirect, ~128 placeholder directories deleted, clean page inventory.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-audit-cleanup/01-RESEARCH.md
@thin-content-report.json
@netlify.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove off-topic fishing charters page (AUDIT-03)</name>
  <files>
netlify.toml
  </files>
  <action>
1. Confirm the off-topic page exists: check for `app/poseidon-fishing-charters-premier-angling-adventures-in-ruskin-fl/page.tsx` (or similar slug -- research says it exists).

2. Add a 301 redirect in `netlify.toml` BEFORE the catch-all `/*` redirect at the bottom. Add it in the redirect section with the other redirects:
```toml
[[redirects]]
  from = "/poseidon-fishing-charters-premier-angling-adventures-in-ruskin-fl/"
  to = "/locations/ruskin-fl/"
  status = 301
```
Target is `/locations/ruskin-fl/` since the fishing page was about Ruskin FL -- redirect to the relevant location page to preserve any backlink equity.

3. Delete the page directory: `rm -rf app/poseidon-fishing-charters-premier-angling-adventures-in-ruskin-fl/`

4. Check for any internal links pointing to this page across the codebase:
```bash
grep -r "poseidon-fishing" app/ --include="*.tsx" -l
```
If found, remove those links.

5. Also scan for any other obviously off-topic pages. Research found only this one, but verify by checking for pages about topics unrelated to construction/restoration (fishing, cooking, travel, etc.). A quick check:
```bash
grep -rl "fishing\|charter\|angling" app/ --include="*.tsx"
```
  </action>
  <verify>
1. `ls app/poseidon-fishing-charters*` returns "No such file or directory"
2. `grep "poseidon-fishing" netlify.toml` shows the 301 redirect
3. `grep -r "poseidon-fishing" app/ --include="*.tsx"` returns nothing
  </verify>
  <done>The fishing charters page is deleted, a 301 redirect to /locations/ruskin-fl/ exists in netlify.toml, and no internal links point to the removed page.</done>
</task>

<task type="auto">
  <name>Task 2: Delete placeholder page files that have existing redirects (AUDIT-02 partial)</name>
  <files>
scripts/cleanup-placeholder-pages.js
  </files>
  <action>
Create `scripts/cleanup-placeholder-pages.js` that:

1. Reads `thin-content-report.json` to get the list of placeholder pages (category: "placeholder", wordCount: 34, title contains "Redirecting")
2. Reads `netlify.toml` to extract all existing redirect `from` paths
3. Cross-references: for each placeholder page, check if its URL has a matching redirect in netlify.toml
4. For placeholders WITH existing redirects: delete the `app/{slug}/` directory (both page.tsx and the directory)
5. For placeholders WITHOUT redirects: LOG them but do NOT delete (these need manual redirect decisions -- flag for Plan 03)
6. Output a summary:
   - Deleted: N pages (had existing redirects)
   - Flagged: N pages (no redirect found -- need attention)
   - List of flagged pages with their URLs

Run the script:
```bash
node scripts/cleanup-placeholder-pages.js
```

IMPORTANT safety checks:
- NEVER delete a page that doesn't match the placeholder criteria (34 words, "Redirecting" title)
- NEVER delete a page without verifying a redirect exists for its URL
- Log every deletion for auditability
- The script should do a DRY RUN first (print what it would delete), then prompt or have a `--execute` flag to actually delete

Run with dry run first, review output, then run with execute:
```bash
node scripts/cleanup-placeholder-pages.js --dry-run
node scripts/cleanup-placeholder-pages.js --execute
```

Expected: ~120-128 placeholder directories deleted (most should have redirects since netlify.toml has 147 redirect blocks).
  </action>
  <verify>
1. Count remaining page.tsx files: `find app/ -name "page.tsx" | wc -l` -- should be ~367-376 (496 minus ~120-128 placeholders minus 1 fishing page)
2. Verify no placeholder content remains: `grep -rl "Redirecting" app/ --include="*.tsx" | head -5` -- should return very few or zero results
3. Spot-check 3 deleted pages: confirm their redirect exists in netlify.toml
4. Check script output for any "flagged" (no redirect) placeholders -- note these for Plan 03
  </verify>
  <done>All placeholder pages with existing redirects are deleted. Any placeholders without redirects are logged for follow-up. Page count is reduced by ~120-128.</done>
</task>

</tasks>

<verification>
1. AUDIT-03 complete: `ls app/poseidon*` fails, `grep poseidon netlify.toml` shows redirect
2. AUDIT-02 partial: `find app/ -name "page.tsx" | wc -l` shows reduced count (~367-376)
3. No broken internal links: `grep -r "poseidon" app/ --include="*.tsx"` returns nothing
</verification>

<success_criteria>
- Off-topic fishing charters page is deleted with 301 redirect in place
- 120+ placeholder page directories are deleted (only those with confirmed redirects)
- Any placeholders without redirects are documented for Plan 03
- Page count is reduced from ~496 to ~367-376
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-audit-cleanup/01-02-SUMMARY.md`

IMPORTANT: Record the exact final page count and list of any flagged (unredirected) placeholder pages in the summary -- Plan 03 depends on this information.
</output>
