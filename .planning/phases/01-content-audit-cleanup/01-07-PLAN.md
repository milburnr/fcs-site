---
phase: 01-content-audit-cleanup
plan: 07
type: execute
wave: 5
depends_on: ["01-05"]
files_modified:
  - scripts/rewrite-descriptions.js
autonomous: true

must_haves:
  truths:
    - "All meta descriptions on indexed pages are 150-160 characters"
    - "Every meta description ends with a CTA (call, request, get, schedule, etc.)"
    - "No TypeScript syntax errors were introduced by the rewrite script"
  artifacts:
    - path: "scripts/rewrite-descriptions.js"
      provides: "Script that reads seo/description-rewrites.json and applies changes to page.tsx files"
  key_links:
    - from: "scripts/rewrite-descriptions.js"
      to: "seo/description-rewrites.json"
      via: "Script reads the rewrite records generated by Plan 05"
      pattern: "description-rewrites.json"
    - from: "scripts/rewrite-descriptions.js"
      to: "app/*/page.tsx"
      via: "Script opens each page.tsx, finds description in metadata, replaces old with new"
      pattern: "description:"
---

<objective>
Create and run a script to apply the description rewrites from Plan 05 to all ~278 page.tsx source files.

Purpose: Plan 05 generated the rewrite data (seo/description-rewrites.json). This plan creates an automated script to apply those rewrites safely across all page.tsx files. The script approach ensures no manual file-by-file editing -- one script reads the JSON and updates all files programmatically.
Output: All page.tsx files updated with correct-length descriptions, verified by build.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-content-audit-cleanup/01-05-SUMMARY.md
@seo/description-rewrites.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create and run the description rewrite script</name>
  <files>
scripts/rewrite-descriptions.js
  </files>
  <action>
Create `scripts/rewrite-descriptions.js` that:

1. Reads `seo/description-rewrites.json`
2. For each rewrite entry:
   a. Opens the page.tsx file at the specified path
   b. Finds the `description:` property in the metadata export
   c. Replaces the old description string with the new one
   d. Preserves all other metadata and file content
3. Handles both single-line and multi-line description strings
4. Does NOT break TypeScript syntax (watch for quote escaping, trailing commas)
5. Logs: files updated, files skipped (description already correct or file not found), errors

Safety checks:
- Before writing, verify the old_description matches what's in the file (guard against stale data)
- If old_description doesn't match, SKIP and log a warning (page was already modified)
- Support `--dry-run` flag to preview changes without writing

Run:
```bash
node scripts/rewrite-descriptions.js --dry-run
# Review output -- confirm expected file count, no errors
node scripts/rewrite-descriptions.js
```

The script handles all ~278 files programmatically. Claude does NOT need to open each file individually -- the script does the work.
  </action>
  <verify>
1. Script reports ~278 files updated with 0 errors
2. Spot-check 10 random page.tsx files (2 per type: service-location, article, hub, location, other) -- description length is 150-160 chars
3. `npm run build` succeeds -- no TypeScript syntax errors introduced
4. Scan all page.tsx metadata for descriptions over 160 chars -- should find zero: `node -e "const fs=require('fs');const glob=require('glob');const files=glob.sync('app/*/page.tsx');let over=0;files.forEach(f=>{const c=fs.readFileSync(f,'utf8');const m=c.match(/description:\\s*['\"](.+?)['\"]/);if(m&&m[1].length>160){over++;console.log(f,m[1].length)}});console.log('Over 160:',over)"`
  </verify>
  <done>Script created and executed. All ~278 page.tsx files have updated descriptions at 150-160 chars with CTAs. No TypeScript syntax errors. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final description quality audit</name>
  <files>
seo/description-rewrites.json
  </files>
  <action>
Run a final audit across ALL indexed page.tsx files (not just the ones rewritten) to catch any remaining description issues:

1. Scan every page.tsx for its description
2. Flag any description over 160 chars (should be zero after Task 1)
3. Flag any description under 120 chars -- these are too short for SERP optimization but were not part of the oversized rewrite batch
4. Log the under-120 results to `seo/description-rewrites.json` under a new `"short_descriptions"` key:
```json
{
  "short_descriptions": {
    "count": 12,
    "note": "Descriptions under 120 chars -- candidates for future expansion. Not blocking for Phase 1.",
    "pages": [
      {"path": "app/some-page/page.tsx", "url": "/some-page/", "length": 95, "description": "..."}
    ]
  }
}
```

This is documentation only -- do NOT modify any short descriptions in this plan. Short description expansion is a Phase 2+ concern.
  </action>
  <verify>
1. Zero descriptions over 160 chars across all indexed pages
2. Short descriptions are logged in `seo/description-rewrites.json` under `short_descriptions` key
3. `short_descriptions.count` is a number (may be 0 if none found)
  </verify>
  <done>Final audit confirms zero oversized descriptions. Short descriptions documented for future work. AUDIT-07 complete.</done>
</task>

</tasks>

<verification>
AUDIT-07 complete:
1. Scan all page.tsx files for description length -- none over 160 chars
2. `seo/description-rewrites.json` documents all changes made and short descriptions noted
3. Build still works after changes
</verification>

<success_criteria>
- Zero meta descriptions over 160 characters on indexed pages
- All rewritten descriptions end with a CTA
- No TypeScript syntax errors introduced
- Before/after record preserved in seo/description-rewrites.json
- Short descriptions (<120 chars) documented for future work (not blocking)
</success_criteria>

<output>
After completion, create `.planning/phases/01-content-audit-cleanup/01-07-SUMMARY.md`

Record: total descriptions rewritten by script, any pages skipped, short descriptions count, build pass/fail.
</output>
