---
phase: 03-schema-trust-signals
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fix-nap-consistency.js
  - app/**/*.tsx (40+ pages with wrong phone, 6+ with wrong license)
  - lib/constants.ts
autonomous: true

must_haves:
  truths:
    - "Every page on the site displays only the canonical phone number (813) 420-7561 -- zero instances of (813) 906-1302"
    - "Every page on the site displays only the canonical license number CBC1262722 -- zero instances of CGC1529395, CGC1522434, CGC1532648, or CGC prefix"
    - "The business name is exactly 'Florida Construction Specialists' everywhere -- no suffix or tagline variations"
  artifacts:
    - path: "scripts/fix-nap-consistency.js"
      provides: "Automated NAP replacement script"
    - path: "lib/constants.ts"
      provides: "Canonical NAP source of truth (already correct, verify only)"
  key_links:
    - from: "scripts/fix-nap-consistency.js"
      to: "app/**/*.tsx"
      via: "find-and-replace of hardcoded phone/license strings"
      pattern: "813.906.1302|CGC1529395|CGC1522434|CGC1532648"
---

<objective>
Fix NAP (Name, Address, Phone) consistency across all pages. Replace 40 hardcoded wrong phone numbers (813-906-1302 -> 813-420-7561), fix 6+ wrong license numbers (various CGC numbers -> CBC1262722), and fix CGC prefix to CBC in all meta descriptions and body text.

Purpose: NAP inconsistency is the #1 trust signal failure -- Google penalizes businesses with conflicting contact information. Wrong license numbers are a potential legal liability.
Output: Zero NAP inconsistencies across all 427+ pages.
</objective>

<execution_context>
@/Users/bo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-schema-trust-signals/03-RESEARCH.md
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all wrong phone numbers and license numbers across site</name>
  <files>
    scripts/fix-nap-consistency.js
    app/**/*.tsx (40+ files with wrong phone, 6+ files with wrong license)
  </files>
  <action>
Create a Node.js script `scripts/fix-nap-consistency.js` that:

1. Recursively scans all `.tsx` files under `app/`
2. Performs these replacements in each file:

**Phone number fixes (40 pages):**
- `(813) 906-1302` -> `(813) 420-7561`
- `813-906-1302` -> `813-420-7561`
- `813.906.1302` -> `813.420.7561`
- `8139061302` -> `8134207561`
- `+1-813-906-1302` -> `+1-813-420-7561`

**License number fixes (6+ pages):**
- `CGC1529395` -> `CBC1262722`
- `CGC1522434` -> `CBC1262722`
- `CGC1532648` -> `CBC1262722`

**CGC prefix fixes in meta descriptions and body text:**
- `CGC CBC1262722` -> `CBC1262722` (redundant prefix)
- Any remaining `CGC` followed by digits that is NOT a correct license reference should be reviewed

3. Logs each file and replacement made
4. Reports total changes

Run the script. Verify with grep that zero instances of the wrong patterns remain.

Also check `lib/constants.ts` to confirm the canonical values are correct:
- `phone: "(813) 420-7561"` -- should already be correct
- `licenseNumber: "CBC1262722"` -- should already be correct

Do NOT change any constants.ts values (they're already correct). The problem is hardcoded strings in page files.
  </action>
  <verify>
Run these greps and confirm zero matches:
```bash
grep -r "906-1302\|906\.1302\|9061302" app/ --include="*.tsx" | wc -l  # Should be 0
grep -r "CGC1529395\|CGC1522434\|CGC1532648" app/ --include="*.tsx" | wc -l  # Should be 0
```
  </verify>
  <done>Zero instances of wrong phone number (813-906-1302) or wrong license numbers (CGC1529395, CGC1522434, CGC1532648) exist in any .tsx file. The only phone number on the site is (813) 420-7561 and the only license number is CBC1262722.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix remaining CGC prefix issues in meta descriptions</name>
  <files>app/**/*.tsx (pages with CGC prefix in descriptions)</files>
  <action>
After running the main fix script, do a targeted audit for any remaining CGC prefix usage:

1. Grep for `CGC` across all page.tsx files (case-sensitive)
2. For each match, determine if it's:
   - A wrong license number (already fixed by Task 1)
   - The correct license with wrong prefix (e.g., "CGC CBC1262722" or "CGC 1262722") -- fix to "CBC1262722"
   - A legitimate reference to CGC license type in educational content -- leave as-is but verify context

3. Also audit for any phone numbers in `tel:` href attributes that might use the wrong number:
   ```
   grep -r 'tel:.*906' app/ --include="*.tsx"
   ```

4. Check that `BUSINESS_INFO.phone` and `BUSINESS_INFO.phoneRaw` are used in Header, Footer, and StickyMobileCTA instead of hardcoded numbers (already confirmed in research -- these components use constants).

Report findings and fix any remaining issues.
  </action>
  <verify>
```bash
# Should find ONLY legitimate educational references to CGC (the license type), not wrong license numbers
grep -rn "CGC" app/ --include="*.tsx" | grep -v "node_modules"
# Should find zero wrong phone numbers in tel: attributes
grep -r 'tel:.*906' app/ --include="*.tsx" | wc -l  # Should be 0
```
  </verify>
  <done>All CGC prefix issues resolved. Every license reference on the site uses "CBC1262722". No tel: links contain the wrong phone number. Header, Footer, and StickyMobileCTA confirmed using BUSINESS_INFO constants.</done>
</task>

</tasks>

<verification>
1. `grep -r "906-1302\|906\.1302\|9061302" app/ --include="*.tsx" | wc -l` returns 0
2. `grep -r "CGC1529395\|CGC1522434\|CGC1532648" app/ --include="*.tsx" | wc -l` returns 0
3. `grep -r 'tel:.*906' app/ --include="*.tsx" | wc -l` returns 0
4. `npm run build` succeeds (no TypeScript errors introduced)
</verification>

<success_criteria>
- Zero instances of wrong phone number (813) 906-1302 in any format across all pages
- Zero instances of wrong license numbers (CGC1529395, CGC1522434, CGC1532648) anywhere
- All license references use CBC1262722 with correct CBC prefix
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-schema-trust-signals/03-01-SUMMARY.md`
</output>
